#include <stdio.h>
int main(int argc, char const *argv[])
{
    // video 7
    // Operators
    // float vali value dal ke practice karna
    //types [+,-,*,/],[==,&&,||,!],[&,|,^],(^)is sign ko exclusive XOR bolte hai
    //~ binary one`s complement operator bolte hai
    //[+=] int a=7 hai to a+=1 me ye answer 8 honga isme or bhi types hai
    int a,b;
    a=34;
    b=6;
    
    printf("a + b =%d\n", a+b);
    printf("a - b =%d\n", a-b);
    printf("a * b =%d\n", a*b);
    printf("a / b =%d\n", a/b);
    
    
     return 0;
}
