#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a = 8;
    int b = 80;

    // printf("a + b=%d\n", a + b);
     printf("a / b=%d\n", a / b);
    // printf("a - b=%d\n", a - b);
    // printf("a * b=%d\n", a * b);
    return 0;
}
